name: CI Pipeline - Compilacao Java

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v4

    - name: Configurar JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Preparar diretórios
      run: |
        mkdir -p bin
        mkdir -p lib

   - name: Compilar o projeto
      run: javac -encoding windows-1251 -d bin -cp "lib/*" $(find src -name "*.java")
      
    - name: Verificar sucesso da compilação
      run: echo "Compilação concluída com sucesso! Nenhuma quebra no código."
